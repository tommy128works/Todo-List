// i will need a global array to store projects
const createProject = (title) => {
  let toDos = ["hi"];
  // add todo to project
  const addToDo = (toDo) => {
    toDos
  }
  // return all todo

  return { title, toDos };
};

export default createProject;

